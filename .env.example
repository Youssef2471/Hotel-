# Copy to .env and fill with strong values. Never commit .env.
# PowerShell:  $env:JWT_SECRET_KEY="..." ; $env:AES_KEY="..." (AES_KEY must be 32 characters)

JWT_SECRET_KEY=
AES_KEY=
DATABASE_PATH=database.db
